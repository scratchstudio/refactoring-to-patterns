package encapsulate_classes_with_factory.before;

public class DefaultDescriptor extends AttributeDescriptor {

    public DefaultDescriptor(String remoteId,
                             Class<? extends Client> aClass,
                             Class<?> type) {
        super(/* something */);
    }
}
