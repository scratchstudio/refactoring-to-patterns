package encapsulate_classes_with_factory.after;

public class DefaultDescriptor extends AttributeDescriptor {

    protected DefaultDescriptor(String remoteId,
                             Class<? extends encapsulate_classes_with_factory.after.Client> aClass,
                             Class<?> type) {
        super(/* something */);
    }
}
